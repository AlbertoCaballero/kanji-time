<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.KanjiTime">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".KanjiWidgetProvider"
            android:exported="true"
            android:label="@string/widget_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/kanji_widget_info_1h" />
            <meta-data
                android:name="kanji_widget_info_2h"
                android:resource="@xml/kanji_widget_info_2h" />
            <meta-data
                android:name="kanji_widget_info_6h"
                android:resource="@xml/kanji_widget_info_6h" />
            <meta-data
                android:name="kanji_widget_info_12h"
                android:resource="@xml/kanji_widget_info_12h" />
            <meta-data
                android:name="kanji_widget_info_24h"
                android:resource="@xml/kanji_widget_info_24h" />
        </receiver>
    </application>

</manifest>
